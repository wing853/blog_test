<!-- 푸터 시작 -->
<footer class="footer-natural">
    <div class="container text-center">
        <span class="footer-brand">The Archive.</span>
        <div class="footer-links">
            <a href="#" class="footer-link">Journal</a>
            <a href="#" class="footer-link">Privacy Policy</a>
            <a href="#" class="footer-link">Contact</a>
        </div>
        <p class="copyright text-muted mb-0">© 2026 Archive. Thoughtfully designed for readers.</p>
    </div>
</footer>

<!-- Bootstrap 5 JS Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<style>
    /* 푸터가 항상 바닥에 붙어있게 만드는 핵심 설정 */
    .footer-natural {
        background-color: var(--bg-soft);
        border-top: 1px solid var(--border-color);
        padding: 4rem 0;
        margin-top: auto; /* 중요: 위쪽 여백을 자동으로 채워 바닥으로 밀어냄 */
        width: 100%;
    }

    .footer-brand {
        font-family: 'Lora', serif;
        font-weight: 700;
        font-size: 1.5rem;
        display: block;
        margin-bottom: 1rem;
        color: var(--text-black);
    }

    .footer-links {
        margin-bottom: 1.5rem;
    }

    .footer-link {
        color: var(--text-light);
        text-decoration: none;
        font-size: 0.85rem;
        margin: 0 12px;
        transition: 0.2s;
    }

    .footer-link:hover {
        color: var(--point-green);
        text-decoration: underline;
    }

    .copyright {
        font-size: 0.75rem;
        letter-spacing: 0.5px;
    }
</style>
</body>
</html>